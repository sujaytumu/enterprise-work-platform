{
  "name":"enterprise-work-platform",
  "version":"1.0.0",
  "private":true,
  "engines":{"node":"20.x"},
  "scripts":{
    "start":"node server.js",
    "build":"npm ci --omit=dev && cd client && npm ci --legacy-peer-deps && npm run build"
  },
  "dependencies":{
    "express":"^4.21.2",
    "pg":"^8.13.1"
  }
}